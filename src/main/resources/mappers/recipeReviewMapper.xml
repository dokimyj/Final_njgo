<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="recipeReviewMapper">
	<select id="list" parameterType="java.util.HashMap" resultType="com.njgo.dto.RecipeReviewDTO">
		select * from (select ROWNUM R, C.* from (select * from RECIPE_REVIEW where RECIPENUM=#{reply.num} order by NUM desc) C) where R between #{reply.listInfo.firstRow} and #{reply.listInfo.lastRow}
	</select>
</mapper>